% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dispatch.R
\name{dispatch}
\alias{dispatch}
\title{Dispatch a compute request}
\usage{
dispatch(mode, fn, args = list(), seed = NULL, registry = list())
}
\arguments{
\item{mode}{Character scalar identifying a registry "namespace"
(e.g., \code{"builtin"}, \code{"pkg"}, \code{"snippet"}).}

\item{fn}{Character scalar naming the allowed function within \code{mode}.}

\item{args}{A list of arguments passed to the handler (default: empty list).}

\item{seed}{Optional integer. When supplied, the RNG is seeded for
deterministic computations.}

\item{registry}{A nested named list of handlers. Expected shape:
\code{registry[[mode]][[fn]]} is a function that accepts a single list \code{args}.}
}
\value{
The handler's return value (typically a list).
}
\description{
Routes a request to an allow-listed handler function based on \code{mode} and \code{fn}.
The dispatcher is responsible for (optional) RNG seeding and calling the
appropriate handler in \code{registry}.
}
\examples{
reg <- list(builtin = list(add1 = function(args) list(x = (args$x \%||\% 0) + 1L)))
dispatch("builtin", "add1", list(x = 2L), registry = reg)

}
